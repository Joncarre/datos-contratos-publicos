# Copia este fichero a `.env` y ajusta a tu máquina. NUNCA subas `.env` al repo.

# Raíz de los datos locales (137 GB de ATOM crudos + datos procesados)
DATA_ROOT=./data

# Raíz de los datos procesados (bronze/silver/gold)
PROCESSED_ROOT=./data/_processed
